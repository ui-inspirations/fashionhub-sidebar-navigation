import { MouseEvent } from 'react'

/*
|--------------------------------------------------------------------------
| Events
|--------------------------------------------------------------------------
|
| ...
|
*/

export const onClickOnLink = (event: MouseEvent<HTMLAnchorElement>) => {
  event.preventDefault()
}
